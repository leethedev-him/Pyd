name: Build Windows Executable

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build .exe with PyInstaller
        shell: pwsh
        run: |
          $data = & python -c "import latex2mathml, os; print(os.path.join(os.path.dirname(latex2mathml.__file__), 'unimathsymbols.txt'))"
          pyinstaller --onefile --windowed `
            --add-data "$data;latex2mathml" `
            --hidden-import latex2mathml.symbols_parser `
            --hidden-import latex2mathml.commands `
            --hidden-import latex2mathml.tokenizer `
            --hidden-import latex2mathml.walker `
            --hidden-import latex2mathml.exceptions `
            --name "LatexDocxFixer" main.py

      - name: Upload .exe as artifact
        uses: actions/upload-artifact@v4
        with:
          name: LatexDocxFixer
          path: dist/LatexDocxFixer.exe

  build-installer:
    needs: build
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download .exe
        uses: actions/download-artifact@v4
        with:
          name: LatexDocxFixer
          path: dist

      - name: Install Inno Setup
        shell: pwsh
        run: |
          choco install innosetup --no-progress -y

      - name: Build installer
        shell: cmd
        run: |
          "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss

      - name: Upload installer as artifact
        uses: actions/upload-artifact@v4
        with:
          name: LaTeXDocxFixer_Setup
          path: LaTeXDocxFixer_Setup.exe
